{
  "name": "blink-vs-public-bsc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Experiment comparing transaction inclusion (speed, frontrun exposure, backrun opportunity) between the blink private relay and the public mempool on BNB Smart Chain.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "prepare:wallets": "tsx src/prepare.ts",
    "experiment": "tsx src/experiment.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
